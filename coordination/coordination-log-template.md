# Coordination log — <recipient> — <YYYY-MM-DD>

Copy this file to `coordination-log-<recipient-slug>-<YYYY-MM-DD>.md` when the first message goes out. Append rows below for every subsequent round.

## Recipient

- Name / group:
- Verified contact method (from primary source, on this send day):
- Source URL:
- Date re-verified:

## Outgoing message

- Sent at (ISO 8601 UTC):
- Subject:
- From:
- Draft template used (relative path):
- Any edits made from the template (short bullet list):
  -
- Attachments / links:
  -
- Full sent text (paste verbatim as `> ` quoted block below or attach as separate file):

> (paste sent message here)

## Response

- Received at (ISO 8601 UTC):
- Response medium (email, list reply, GitHub issue, ...):
- Response summary (2-5 sentences, plain-language, no interpretation):
- Full response text (paste verbatim as `> ` quoted block, or attach):

> (paste response here if received)

## Pause point

Do NOT act on the response until the maintainer has reviewed. Record the pause point here:

- Reviewer:
- Reviewed at:
- Decision (choose one):
  - No further action
  - Follow up with additional questions (draft below)
  - Re-scope PAMSPEC based on feedback (open ADR)
  - Publish coordination outcome in PAMSPEC repo (link the PR)
  - Other:

## Next action (if any)

- Description:
- Owner:
- Target date:
- Blocker(s):

---

## Notes

- Do NOT paste any recipient's private contact information into any file other than a coordination-log file, and even there, only the fields needed to identify the correspondence (name + venue, not third-party details).
- Do NOT publish outreach outcomes on public lists as coordination "outcomes" until the recipient has explicitly confirmed the framing.
- Do NOT extract private contact information from replies for reuse in unrelated outreach.
- Recipient replies are their words, not PAMSPEC's; do not quote selectively in ways that misrepresent their position.
