# Human-readable dependency set for the PAMSPEC-vs-Mem0 validation sprint.
# mem0ai is exact-pinned; chromadb and sentence-transformers use ranges.
# For a package-exact reproduction of V08.1 test time, use
# validation/requirements-lock.txt instead.
# Do NOT modify the mem0ai pin without recording the change in
# reports/real-framework-validation-report.md — a version bump could
# change which PAMSPEC requirements Mem0 satisfies natively.
mem0ai==2.0.12
chromadb>=0.5.0,<1.0
sentence-transformers>=3.0,<4.0
