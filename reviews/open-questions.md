# Open Questions Register

- Should JSON Schema become a normative dependency in a future revision?
- Which object-type-specific transition restrictions need implementation evidence?
- What cryptographic integrity metadata, if any, should be mandatory?
- How should implementation reports describe eventual consistency of Derived Indexes?
- Should protocol bindings be split into separate future documents?
- What canonical serialization should be used if descriptor or content hashes become interoperable identifiers?
