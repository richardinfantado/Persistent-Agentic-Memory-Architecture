# Maintainer notes

Small operational notes that don't belong in ADRs or the changelog.

## Pending wording corrections for R6a outreach

The coordination package on branch `coordination/06a-adjacent-author-outreach-prep` is preserved intact per current review. **Before any message in that branch is sent, apply the following wording correction across every draft:**

- Replace `"still-internal project"` with:
  `"public, pre-submission work in progress"`

The repository is public. Calling PAMSPEC "internal" is inaccurate.

The correction is recorded here (not applied on the R6a branch itself) so the branch remains intact for review and the correction is not forgotten when sending is later authorized.

## Current pause list

Do not resume any of the following without explicit new authorization:

- R4b (experimental capability moves)
- R4c (optional profile directories)
- R5 (Lite semantic simplification)
- R6 outreach sending (drafts prepared, not sent)
- R8 (draft-latest rewrite)
- TypeScript clean-room implementation
- Memory Bundle work
- IETF submission preparation
- Numbered revision generation
- New ADRs, crosswalk revisions, classification matrices, or execution plans

The active work is `validation/08-real-framework-proof` — implementation-and-evidence, not governance.
