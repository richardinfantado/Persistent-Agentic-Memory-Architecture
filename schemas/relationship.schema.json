{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/pama/schemas/relationship.schema.json",
  "title": "PAMSPEC Relationship Candidate Schema",
  "description": "Unstable pre-0.1 review schema for draft-infantado-agent-memory-architecture-00; candidate interoperability model, not a frozen protocol contract.",
  "$defs": {
    "draftStatusWarning": {
      "const": "Unstable pre-0.1 review schema for draft-infantado-agent-memory-architecture-00; candidate interoperability model, not a frozen protocol contract."
    },
    "id": {
      "type": "string",
      "minLength": 1
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "lifecycle": {
      "enum": [
        "scratch",
        "candidate",
        "active",
        "superseded",
        "deprecated",
        "archived",
        "redacted",
        "deleted"
      ]
    },
    "validation": {
      "enum": [
        "unverified",
        "corroborated",
        "disputed",
        "rejected"
      ]
    }
  },
  "type": "object",
  "required": [
    "draftStatusWarning",
    "relationship_id",
    "relationship_type",
    "source_object_id",
    "target_object_id",
    "scope_id"
  ],
  "properties": {
    "draftStatusWarning": {
      "$ref": "#/$defs/draftStatusWarning"
    },
    "relationship_id": {
      "$ref": "#/$defs/id"
    },
    "relationship_type": {
      "type": "string"
    },
    "source_object_id": {
      "$ref": "#/$defs/id"
    },
    "source_version_id": {
      "type": "string"
    },
    "target_object_id": {
      "$ref": "#/$defs/id"
    },
    "target_version_id": {
      "type": "string"
    },
    "scope_id": {
      "$ref": "#/$defs/id"
    },
    "target_scope_id": {
      "type": "string"
    },
    "policy_basis": {
      "type": "string"
    },
    "provenance": {
      "type": "object"
    }
  },
  "additionalProperties": false
}
